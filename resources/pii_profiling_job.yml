# PII detection profiling -- multi-model vLLM sweep on a single A100 node.
#
# DISABLED: this job requires a classic GPU (A100) cluster. The shm-skunkworks FEVM
# is serverless-only ("Only serverless compute is supported"), and the previously
# referenced cluster (0309-232400-mqol8s67) no longer exists, so this job cannot be
# deployed or run here. The resource is commented out to keep `bundle deploy` clean.
# To restore, run on a workspace that allows classic GPU clusters and re-add an
# `existing_cluster_id` (or a `new_cluster` GPU spec) to each task.
#
# resources:
#   jobs:
#     pii_profiling_job:
#       name: "[vLLM] PII detection profiling (multi-model)"
#       tasks:
#         - task_key: qwen3_4b
#           existing_cluster_id: <A100_CLUSTER_ID>
#           notebook_task:
#             notebook_path: ../vllm/pii_detection_profiling.ipynb
#             base_parameters:
#               model_id: "Qwen/Qwen3-4B"
#               num_rows: "500"
#               max_tokens: "500"
#               output_catalog: "shm"
#               output_schema: "default"
#           timeout_seconds: 3600
#         # ... qwen3_8b, qwen3_14b (see git history for the full original)
