# Minimal config for the agent-on-App (self-contained deployment).
# Mirrors agents/config.py -- edit to point at your own catalog/schema.
CATALOG = "shm_skunkworks_catalog"
SCHEMA = "claims_demo"
CHAT_MODEL = "databricks-claude-sonnet-4-5"
